 (ns clb.core
  (:gen-class)
  (:require [clojure.string :as st]
            [clb.rss-parse :as rsp]
            [clojure.pprint :as pp]
            [clojure.edn :as edn]
            [clb.feed-process :as fp]))

()

(defn -main
  "Cows"
  [& args]

  )
